# DEV Source Live Smoke

- 일시: 2026-07-21
- 대상: public Forem V1 `GET https://dev.to/api/articles?per_page=30`
- 호출 경계: `Accept: application/vnd.forem.api-v1+json`, API key 없음, redirect 금지, exact `dev.to` host

Production build의 `DevAdapter`와 공통 bounded HTTP client를 직접 사용했다. Raw response, article 제목·ID·URL·tag·작성자 정보는 출력하거나 기록하지 않고 정규화 결과의 저카디널리티 집계만 확인했다.

| 항목 | 결과 |
|---|---:|
| 정규화 item | 30 |
| `DEV` source | 30 |
| `https://dev.to/` link | 30 |
| 예상 읽기 시간 제공 | 30 |
| `REACTIONS` engagement 제공 | 30 |
| `DEV Community` attribution | 전체 통과 |

이 결과는 Task 29A 시점의 실제 source parsing·attribution·HTTPS link smoke 증거다. 외부 feed는 시간에 따라 달라지므로 지속적 availability를 보장하지 않으며, authenticated `/discover` browser flow, PostgreSQL stale 전환, 전체 접근성 검사를 대신하지 않는다.
