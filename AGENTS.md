# AISideQuest agent instructions

Before changing code, read `docs/ai/README.md` and the domain document it routes to.

Treat database constraints, server types, and tests as stronger evidence than historical plans. When behavior changes, update code, tests, the relevant `docs/ai/` contract, and any affected Korean document in `docs/` in the same change.

Never introduce prompt, AI response, source code, diff, transcript, tool input/output, or local-path collection. Never mark task 20 complete without real staging/production evidence and the documented pilot sample.

